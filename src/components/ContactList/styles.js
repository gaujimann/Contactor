import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    fontSize: 16,
    fontWeight: 'bold',
    backgroundColor: 'rgb(100, 100, 100)',
    color: 'white',
    padding: 4,
    paddingLeft: 16,
  },
});
