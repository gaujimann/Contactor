import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    height: 32,
    width: 300,
    color: 'white',
    borderColor: 'white',
    borderStyle: 'solid',
    borderWidth: 1,
    borderRadius: 16,
    padding: 8,
  },
});
