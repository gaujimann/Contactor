import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    height: 32,
    width: 300,
    color: 'white',
    borderColor: 'gray',
    borderStyle: 'solid',
    borderWidth: 1,
    borderRadius: 8,
    padding: 8,
  },
});
